enum ViewState { Idle, Busy }

enum SectionType { article, updates, liveRate }

enum PaymentStatus {
  unpaid,
  pending,
  partial,
  paid,
  overdue,
  refunded,
  failed,
  cancelled,
  disputed,
}

enum Gender {
  male,
  female,
}

enum IDType {
  voterId,
  drivingLicense,
  aadhaar,
  rationCard,
}

enum CompanyType {
  HUF,
  LLP,
  Partnership,
  Proprietorship,
  Public_Ltd,
  Pvt_Ltd,
}
